{
  "name": "fims-chat-cli",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "features": {
    "ghcr.io/devcontainers/features/github-cli:1": {}
  },
  "postCreateCommand": "gh extension install github/gh-copilot --force",
  "postStartCommand": "bash -lc 'git pull --ff-only || echo \"git pull failed; continuing with submodule update.\"; git submodule update --init --recursive'"
}
